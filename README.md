# Flash card
Express flash card starter project

1. Clone the repository
2. Run `npm install`
3. For file changes watch, run `npm install -g nodemon`
3. For local development, start the server `nodemon app.js`
4. The app runs on: http://localhost:3000
